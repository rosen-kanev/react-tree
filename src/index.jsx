import React from 'react';
import ReactDOM from 'react-dom';

import './index.css'; // styles just for the demo

import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
